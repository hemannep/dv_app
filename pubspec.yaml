name: dvapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  google_fonts: ^6.3.0
  hive_flutter: ^1.1.0
  flutter_local_notifications: ^19.4.0
  flutter_localization: ^0.3.3
  image_picker: ^1.1.2
  image: ^4.5.4
  url_launcher: ^6.3.2
  camera: ^0.10.6
  
  # State management and local storage
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1
  permission_handler: ^11.0.1
  
  # Use gal for saving images (more reliable than image_gallery_saver)
  gal: ^2.3.0
  google_mlkit_face_detection: ^0.13.1
  share_plus: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
